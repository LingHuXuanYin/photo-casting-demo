# 默认配置（兜底，所有平台都生效）
# 当 .env_fnOS / .env_dev / .env_local 里没指定某个变量时，使用这里的值

# 运行模式
NODE_ENV=production

# Server 监听（容器内）
PORT=3001
HOST=0.0.0.0

# CORS 允许的 web 来源（生产环境是容器内的 web 端口）
WEB_ORIGIN=http://localhost:10001
