# 本地开发（npm run dev 启动时用）
# 用法：在 server/ 或 web/ 下手动 `export $(cat env_config/.env_dev | xargs)` 然后 npm run dev
# 或者用 dotenv 自动加载

# 运行模式
NODE_ENV=development

# Server 监听
PORT=3001
HOST=0.0.0.0

# 本地 Vite dev server（5173），CORS 允许它
WEB_ORIGIN=http://localhost:5173
